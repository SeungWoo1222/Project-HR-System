# =========================
# Local profile
# =========================

# --- Datasource ---
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3307/hr?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=utf8
spring.datasource.username=hr_app
spring.datasource.password=hrpass

# --- Hikari ---
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2

# --- Logging ---
logging.level.root=INFO
logging.level.org.springframework.web=DEBUG

# --- AWS ---
# LocalStack endpoint (? AWS? ?? ?? LocalStackcxd? ????)
cloud.aws.s3.endpoint=http://localhost:4566

